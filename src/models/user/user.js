/*
function User() {
  this.
}

function PropertyObject(type, required, defaultValue) {
    this.type = type;
    this.required = required;
    this.default = defaultValue;
}

function UserDB() {
  this.firstName = new PropertyObject('string', true, '')
  this.lastName = new PropertyObject('string', true, '')
  this.username = new PropertyObject('string', true, '')
  this.email = new PropertyObject('string', true, '')
  this.id = new PropertyObject('string', true, '')
  this.lastVisit = new PropertyObject('string', true, '')
  this.imageURL = new PropertyObject('string', true, '')
  this.createdAt = new PropertyObject('string', true, '')
}
*/
